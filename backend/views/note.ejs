<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= note.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h2><%= note.title %></h2>
                <span class="badge bg-primary"><%= note.category %></span>
            </div>
            <div class="card-body">
                <div class="note-content">
                    <%= note.content %>
                </div>
            </div>
            <div class="card-footer d-flex justify-content-between">
                <div>
                    <small class="text-muted">Tạo ngày: <%= new Date(note.createdAt).toLocaleDateString('vi-VN') %></small>
                </div>
                <div>
                    <a href="/api/notes/edit/<%= note._id %>" class="btn btn-warning btn-sm">Sửa</a>
                    <a href="/api/notes/view" class="btn btn-secondary btn-sm">Quay lại</a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 